========================================================================
    WIN32 APPLICATION : FindFirstChangeNotification Interview Test App
========================================================================

This project locks up and doesn't display.
It includes a minidump as well as the project files and symbol files.
Figure out why this application is locking up. Fix it so that the application starts, displays its window and exits properly.
